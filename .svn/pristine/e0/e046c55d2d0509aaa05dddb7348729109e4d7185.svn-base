package com.nfdw.service;

import com.nfdw.entity.PageData;
import com.nfdw.pojo.EchartsBasicBean;
import com.nfdw.pojo.EchartsMapBean;

import java.util.List;

public interface ScreenHomeService {
	//获取总卡数和0流量卡数
	PageData getCountCard(PageData pd);

	/**
	 * 获取设备告警数量（严重）
	 * @param pd
	 * @return
	 * @throws Exception
	 */
	PageData getWarningDatas(PageData pd) throws Exception;

	/**
	 * 获取设备告警数量（一般）
	 * @param pd
	 * @return
	 * @throws Exception
	 */
	PageData getGeneralWarningDatas(PageData pd) throws Exception;
	
	/**
	 * 获取设备正常（非告警）数量
	 * @param pd
	 * @return
	 */
	PageData getTermianlUnWarningDatas(PageData pd) throws Exception;
	
	/**
	 * 获取设备不在线数量
	 * @param pd
	 * @return
	 */
	PageData getTermianlOutLineDatas(PageData pd) throws Exception;

	PageData selectEquipmentBusinesstypeCount();

	EchartsMapBean getOnlineRateOfRegion(PageData requestPd) throws Exception;

	PageData getMapInfoNum(PageData pd);

	EchartsBasicBean getFactoryCode(PageData pd) throws Exception;

	List<PageData> findCardFlowData(PageData pd) throws Exception;

}
